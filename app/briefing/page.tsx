import { BriefingForm } from "@/components/briefing/BriefingForm";

/** O link aberto, o mesmo de sempre: sem nada preenchido. */
export default function BriefingPage() {
  return <BriefingForm />;
}
